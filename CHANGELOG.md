# 更新日志

本文件记录用户可见的稳定里程碑。安装包、测试数字和真实租户结论只在完成对应验证后填写，不能用 Mock 结果代替真实连接验收。

## Unreleased — 2026-08-14

### 新增

- Issue #63 建立唯一 [当前状态](docs/current-state.md)、[项目地图](docs/project-map.md) 与 L0–L6 [验证矩阵](docs/verification-matrix.md)。README、AGENTS 和 docs 入口不再复制历史 Issue/PR、测试数字或安装包 hash；历史继续保留在 CHANGELOG/QA。
- Issue #63 复核加固验证语义：目标层级与已取得证据分离，未运行/历史声明不再显示为真实环境已验证；产品源码 fingerprint 作为跨 squash/merge 的 current-state 新鲜度门禁。
- 新增 `npm run docs:check`，校验入口引用、本地链接、文档 manifest、验证措辞、精确产物和 changed-path 文档影响；CI 在 PR 上绑定 base commit 执行。
- 消息中的飞书 Docx、Wiki→Docx 可在主人 OAuth 权限范围内读取为需求背景；Sheet、Base、旧版 Doc、File 和 Slides 当前只识别链接。
- 候选收件箱固定展示“时间范围、背景、希望验证、Describe、为什么被识别”，并区分来源事实、文档内容、AI 推测和未推断出。
- 飞书连接主卡直接展示 OAuth 回调与 scope；折叠权限指南提供图文步骤、开放平台批量权限 JSON、复制和推荐 OAuth scope 自动填入。
- 个人信息流支持自动发现可选私聊，但人员和群聊都默认不关注；主人可逐个选择，或点击“关注所有人”立即启用当前已发现的全部人员。以后新发现的人仍保持关闭，页面不要求填写 `chat_id`。
- 候选收件箱增加可恢复回收站：可将误识别或暂不处理的候选移入回收站、查看并恢复；活动列表隐藏期间不再接受、纠错或重新整理。
- Issue #11 将默认任务维护模式改为“AI 自动维护内部任务”：高置信且唯一归属的后续消息可直接更新需求线程和私人任务；弱关联、歧义、模型降级、推测时间和版本冲突仍进入待确认。
- 任务详情增加“AI 最近修改”：展示前后值、来源、模型、关联/更新置信度、原因和时间，并允许在没有后续修改覆盖时一键撤销。
- 增加全局“AI 自动维护 / 仅建议”模式和单任务暂停；暂停期间来源继续保存并形成待处理记录。
- 任务详情提供统一编辑入口，覆盖标题、Describe、下一步、风险、等待原因、状态和私人计划时间，并通过任务版本防止旧页面覆盖新修改。
- 任务记忆增加“清理并重建”，参考路径增加解除绑定；系统只清理明确托管的投影文件，不删除未知附件或真实工作目录。
- Issue #13 增加待确认候选归并与主人主体识别：AI 同时判断是否属于同一需求、哪条是主人需要推进的具体工作，并把其他来源标记为背景、约束或流程咨询。
- 主人明确选择“保留为两件事”后会持久阻止同一候选对再次建议或自动归并；旧建议会校验候选、线程、组成员和页面版本，连续消息候选拆分时整批来源一起迁移。
- 候选收件箱只展示一张主体卡，可展开全部来源摘要、更换主体、拆分误归并来源；低置信建议可确认合并或明确保留为两件事。聊天正文仍只保留在本地来源审计中。
- 尚未执行的主人动作会按候选 ID 或正式任务 ID 挂到对应候选卡片的“AI 判断”折叠区，精简显示动作、置信度、执行状态、原因和时间；只有完全无法关联需求的判断才保留顶部提醒，聊天正文不会进入该区域。
- 已接受候选与对应正式任务共享回收状态：从候选页或任务页任一入口删除、恢复，双方都会在同一事务中同步变化；未接受候选仍只处理自身，来源和审计始终保留。
- Issue #15 增加结构失败自动恢复：无效 JSON、缺字段或非法多需求结构不会被固化成“不是需求”，来源由 Runtime 按有限重试与指数退避继续恢复；结构失败不再生成 `0%` 候选卡，旧版“AI 整理待重试”占位也会从候选列表隐藏。
- 疑似漏掉前文时，在当前启用会话和硬起点内进行最多 72 小时、3 页的有界背景补扫；补扫不推进正常同步游标，主人消息只作背景，不会单独生成候选。
- 连续消息可以拆成最多 8 个需求单元；同一原始消息可支撑多个需求，每个单元分别关联需求线程和已有本地任务，每张候选卡代表一个具体需求并显示背景完整性。
- Issue #19 增加主人消息状态机：主人每条消息按时间顺序独立判断，可自动承接候选、确认私人排期、索要资料、继续推进、拒绝或转交未接受候选；主人消息不生成普通候选，但会在唯一目标、来源证据、版本和 Runtime 租约门禁通过后维护私人任务状态、计划、等待原因和时间线。多目标、模糊日期、终态任务、模型降级和版本冲突会安全降级为待确认。
- Issue #23 将连续对话主链改为“消息动作 → 需求线程 → 最小更新”：LLM 负责判断新需求、已有需求更新、上下文、主人动作或待确认，服务端负责匿名候选快照、完整评分、置信度、版本和日期安全门。只有 `new_demand` 创建候选；待接受候选的补充会原地追加到同一 `demand_unit_id` 并生成候选修订；交错需求、低置信或模型降级时建立 `needs_confirmation` 单元和可见提醒，不再凭短句制造重复卡或静默消失。

### 可靠性

- Issue #50 统一排期日历的上海日期合同：服务端按 `Asia/Shanghai` 半开自然日展开跨日任务，结束恰为 00:00 时不进入右侧日期，起止相等作为单日点事件；完成态保留在历史计划日，归档、作废和软删除默认隐藏。单项计划最多覆盖 366 个自然日，任务编辑、主人消息排期、排期纠错、任务更新提案应用与自动更新撤销均复用共享边界，拒绝超限或反向区间；撤销前用单一运行时 schema 强解析 before/after 快照，所有恢复日期只接受严格 ISO datetime 或 `null`，并在任何写入前核对任务、线程、候选与候选修订关联。candidate-linked 更新必须存在唯一 previous revision，previous/applied revision payload 必须分别等于 before/after 候选快照，事务内始终维持恰好一个 current revision 且与 candidate_request 一致；before/after candidate business state 必须同时等于撤销前主表权威状态，恢复 SQL 不从快照更新该状态。普通新来源与主人关联提案保持 candidate-less。损坏、错配或非法 revision 固定返回脱敏 409 且不产生部分写入；合法历史额外字段会被剥离，不传播到恢复 SQL、响应或新任务审计。历史坏锚点逐任务隔离，Calendar API 只返回清洗后的专用展示字段、固定脱敏 warning 和遗漏数量，其他正常任务继续显示。
- Issue #49 修复工作台统计：今天按上海自然日筛选，显式区间跨日时按覆盖日期纳入，completed 不计入待推进且 archived 默认隐藏；候选、今日计划、等待、进行中和逾期分别独立统计，页面不再把有限列表长度当成总数，也不再固定宣称当前使用虚拟数据；演示任务排期同样固定按上海日期生成，避免 CI 或服务器处于 UTC 时漂移到次日。
- Issue #32 统一运行日志、连接健康和诊断导出的递归 fail-closed 脱敏：类型化 schema 同时校验键和值类型，未知/敏感/超长 key 使用对象内唯一短编号，避免原 key 泄漏、碰撞覆盖和 prototype pollution；数组、Error、URL、accessor 与 Proxy 使用不执行用户代码的安全读取路径。共享预算同时约束值节点和输出 key；诊断范围没有扩大，验证只使用合成敏感夹具。

- Issue #80 修复合法历史 SQLite additive lineage 无法识别导致桌面只留下后台进程的问题：支持已证明的列物理顺序差异及三个明确旧 CHECK 缺口，仍拒绝其它 schema 漂移；数据库启动失败现在显示固定脱敏提示、清理核心并退出，不留下无窗口后台进程。验证仅使用合成 fixture，不代表真实用户数据库或 Windows L5/L6。

- 将 DeepSeek 需求判断升级到 `demand_intake_v7` 分阶段小结构：普通消息先判断动作，再按需独立提取需求摘要、正式任务关联、候选归并或任务最小补丁；经稳定 ID 验证的主人消息直接进入小型 `owner_intent`，不再被通用动作路由或关联结构失败阻塞。主人接受、拒绝、转交和确认时间分别由服务端安全门执行；拒绝/转交只进入“已忽略”，不进入回收站。关联不唯一时保留主人判断，并在候选页明确显示“已识别但尚未执行”。
- 工作台与候选收件箱统一过滤旧版结构失败生成的 0% 空占位候选，避免“需要我确认”出现无法打开的“AI 整理待重试”；既有占位可安全进入“已忽略”并归档提醒，原始来源和审计继续保留。
- 主人意图阶段不再接受 `confidence=0`、空摘要或空理由作为“修复成功”；DeepSeek 结构修复会重新读取脱敏的原始对话，避免把明确的承接与时间确认降成 0%。未接受候选阶段的背景/时间追问只保留审计，不再显示成需要主人再次确认的失败动作；同一来源的重复判断只展示最新一条。
- 模型输入增加由服务端稳定主人 ID 验证后的脱敏 `current_sender_role`，需求方的“可以”不能冒充主人确认；模型返回的时间文字只有能在当前分类来源中回查时才会解析，幻觉日期不会进入计划或重要时间。
- 单需求和多需求都校验匿名来源编号。未知或重复来源不能生成候选；失败诊断只保存阶段名、字段路径和错误码，不保存提示词、回复或真实 ID。
- “重新同步”会等待正在执行的后台轮次结束后再真实补跑一次，不再把 `already_running` 误报为配置未执行。
- Issue #21 加固持续对话承接和主人自动动作：同一会话中的短确认、排期询问与资料交接可在有界时间窗内延续唯一需求，明确“另一个/新需求”仍会拆分；普通数字范围不再误判为日期；只有匹配已授权主人稳定 ID 且意图置信度达到 `0.90` 时才允许自动修改任务。
- Issue #23 增加 10 组完整长对话回放，逐轮校验候选数量、线程归属、任务版本和时间线；跨天日期按各自消息时间解析。日期表达与语义角色分离：程序解析具体日期，LLM 标注截止/开始/范围/参考，需求方询问的日期不会直接成为主人计划。
- 修复主人消息同步向飞书发送空 `query` 导致 HTTP 400 的缺陷；通用消息搜索现在会在调用 SDK 前拒绝空白关键词。
- 普通私聊和群聊 `@我` 在缺少对应用户 OAuth scope 时安全跳过本轮，标记为需要管理员批准，不调用消息 API、不推进游标，也不再反复制造 400；重新授权后可从原有水位继续。
- Token 刷新响应未返回 scope 时保留此前已验证的 scope；新授权明确返回空 scope 时仍会清空旧权限，避免把旧授权误当成当前授权。
- 普通私聊改为自动发现、选择式周期读取：分页发现主人 OAuth 可见的既有 P2P，保存对方 `open_id`，内部解析 `chat_id`；主人选择后才处理对方新发消息，不承诺实时或覆盖飞书 API 未返回的会话。
- 群聊改为主人 OAuth 下的选择式读取：按群名选择后只保存真实 `@主人` 命中，不要求机器人在群内；机器人补充群继续独立配置。
- 新发现个人 P2P 默认关闭；“关注所有人”只作用于当前已发现列表，人工选择在刷新、重启和改名后继续保留。重新启用从当前时间建立硬起点，不补收未关注期间消息；新群仍默认关闭。
- 超过 50 个自动私聊时，每轮扫描近期 40 个和最久未扫描 10 个；页面显示部分扫描状态，避免把有界轮转误写成全量实时监控。
- 长分页发现以“扫描开始时间”交接下轮水位，避免会话列表翻页期间新出现的私聊首条消息落在两个扫描窗口之间。
- 人员排除使用差量更新；未变化的群不会被反复停用/启用。旧版 `personIds` 兼容请求只做安全增量启用，不会误排除后台刚发现的新联系人。
- 修复旧 SQLite 在新增 `discovery_rank` 列前提前建索引导致升级启动失败的风险；旧目标与选择状态原样保留。
- 重新 OAuth 时会先暂停旧主人 Runner 和人员/群选择接口；新主人身份确认前不会混用新 Token 与旧主人范围，旧请求返回后会安全丢弃。
- 机器人长连接失败不再阻断主人私聊、群聊、日历和妙记；补充入口可独立重试恢复。
- 推荐 OAuth 权限加入 `contact:user:search` 与 `im:message.p2p_msg:get_as_user`，保留群消息用户身份权限；移除主链不使用的 `search:message`。
- 来源先进入耐久 inbox，文档读取失败不丢来源。
- 增加文档 revision 复核、5 分钟到期刷新、旧结果防覆盖和临时失败 stale 缓存。
- 升级前含 Docx/Wiki 链接但尚无背景记录的历史来源会被限量回填，并与正常到期刷新合计限制为每轮 50 条。
- “来源事实 / 文档内容”标签会回查模型实际收到的文本；无法找到原文的模型概括或幻觉会降为“AI 推测待确认”。
- “下周三前”等表达只填写截止时间，不虚构开始时间；非法日期回退未知。
- 模型输入限制为最多 8 篇文档、单篇 8,000 字、文档正文合计 12,000 字、最终结构化输入 20,000 字。
- Issue #9 后端里程碑补齐受控 PM Runtime：来源幂等、lease、attempt/backoff、checkpoint、取消、失败恢复和 Tool Registry 审计；周期同步遵守退避，显式重试才提前唤醒失败判断。
- 新增需求线程与来源修订链：回复链优先，明确补充才按会话关联，多线程歧义进入待确认，不自动合并。
- 连续消息的预合并仍只覆盖同一会话在同一次完整分页扫描中取回的批次；跨轮询和实时 WebSocket 延迟聚合尚未实现。分别形成的待确认候选现在会再次进入受限语义归并，仍可汇成一个需求。
- 候选归并最多比较 6 个受限候选，要求模型完整评分、目标唯一、候选集完整且快照未变化；高置信且主体角色为 `owner_delivery` 时才自动归并，否则只给建议。
- 归并、主人确认、更换主体、拆分和明确否决均使用事务并写入 `correction_event`；整组接受只生成一个任务，并把所有独立来源挂入任务。
- Issue #9 当时新增任务更新提案：模型只提交带证据和基准版本的结构化 patch，由主人批准后才递增 `task.version`、写入时间线并刷新任务记忆投影；Issue #11 在保留这条安全降级路径的基础上增加高置信内部自动应用。
- SQLite 是唯一事实真源；接受任务后生成可重建的 `task.json`、`brief.md`、`sources.md`、`updates/` 和 `artifacts.json`，投影失败可见、可重试且不回滚正式任务。
- 任务详情新增需求归属确认、更新批准/拒绝、Runtime 手动重试和“打开任务记忆目录”；桌面主进程按 `task_id` 解析并校验真实目录，renderer 不能传入任意路径。
- 同一任务版本的多条待确认提案中，确认一条会让其余旧版本提案失效；待确认、拒绝和失效提案不会进入正式 `updates/` 历史。
- 自动、主人批准和重新整理统一使用同一套提案创建、策略分发与应用事务；AI actor 与主人 actor 分开审计，不能互相伪装。
- 自动更新采用关联置信度与更新置信度双门槛，并同时检查强关联证据、fallback、任务/线程版本、全局模式、单任务暂停和时间确认状态。
- 工作台新增“需要关注”入口：自动应用成功时只保留“AI 已自动维护”提醒，安全门未通过时只保留“请确认更新”；同一来源不得同时保留两类未读提醒。
- 自动修改、人工编辑、纠错、删除、恢复和撤销都会刷新任务记忆；投影失败不回滚 SQLite 正式任务，页面保留错误和幂等重试入口。
- 一键撤销只允许处理仍为任务最新版本的 AI 修改；若已有后续人工或 AI 更新则返回冲突，避免覆盖之后的工作。
- 清理重建按系统托管清单移除过期 `updates`，未知文件继续保留；解除引用只清理 SQLite 绑定和快照。
- Windows 打包默认复用 `npm install` 已安装的同版本 Electron 运行时，避免构建阶段再次联网下载导致短暂网络失败。
- 主人状态机的每个判断都有独立 `owner_decision` 与 Runtime 幂等键；进程重启、重复投递和同一批次多条主人消息不会重复建任务或覆盖前一条主人意图。
- Issue #23 新增 10 个完整对话回放，覆盖无回复链承接、两个需求交错、待接受候选补充、近分歧义、新需求拆分、跨 30 天恢复、日期范围边界、重复投递和低置信降级；跨天日期改为逐条按来源时间锚点解析。

### 安全边界

- 文档只按主人原有权限读取，普通日志不保存完整文档或会议原文。
- 已接受任务的文档变化只提醒系统主人复核，不自动改任务、不新增 Outbox。
- 机器人继续只作为补充入口，不提升为个人信息流主入口。
- 不使用 Chrome 插件、本地飞书数据库或 CLI 常驻进程绕过权限；人员/群选择只在主人 OAuth 实际允许范围内工作。
- 当前尚未提供来源原文/派生摘要永久清除、撤销授权和个人数据导出；运行日志清理不会删除 SQLite 中的来源数据。
- Runtime HTTP 只返回状态、尝试次数和脱敏错误等白名单摘要，不返回 `payload_json`、`result_json`、checkpoint state 或工具结果原文；工具超时不会立即启动可能重叠的进程内重试。
- 候选软删除会归档候选通知并保留来源、审计和正式任务关联；已接受候选会同步处理正式任务，恢复仍不会自动恢复已作废的对外草稿。
- AI 自动化只管理私人 PM 内部记录；回复需求方、发消息、对外排期或完成承诺、执行分析/SQL/Shell、删除真实文件和物理删除 SQLite 数据仍需主人明确操作或继续禁止。
- `completed / archived` 自动更新要求来源有明确完成、取消或不再处理证据，并生成重点提示；任务删除和“这不是需求”不在 AI 自动 patch 白名单内。
- 撤销保留原 AI 修改并追加主人审计事件；投影清理拒绝符号链接、junction/reparse point 和越界路径，不递归删除任务目录。
- 待确认候选发送给模型时只使用匿名 `c1/c2` 与受限摘要；真实候选 ID、线程 ID、快照、路径和凭证不会进入模型输入。未知、重复、漏评或不完整输出会整次安全降级。
- 候选归并只改变私人 PM 内部关联，不覆盖或物理删除任何 `source_event`，不新增 Outbox、不发消息也不执行任务。
- 新增四层稳定标识：`source_event_id`（原始消息）、`demand_unit_id`（具体需求）、`requirement_thread_id`（持续对话线）和 `task_id`（私人正式任务）；归并、拆分、改挂和纠错按需求单元隔离。
- 多需求持久化使用来源 revision fence：模型判断期间来源变化时本轮零写入并重新判断；旧需求单元被新结果替代时只从活动收件箱淘汰，已经接受的任务和来源审计继续保留。

### 验证状态

- `npm run check`：server 342、web 3、desktop 7，全部通过。
- `npm run test:e2e`：37 passed，7 个不适用的手机写库流程按设计 skipped；候选、持续更新、同步忙碌状态和设置流程在桌面与手机视口均通过。
- `npm audit --audit-level=high`：0 vulnerabilities；变更文件未发现真实密钥、令牌或个人绝对路径。
- 正式安装包 `release/Feishu-AI-PM-0.2.0-x64-Setup.exe` 为 101,814,729 字节，SHA-256 `1A2141E124685213DBDD940E525C33CBF693EE8FDDDF460347C4AD374E654950`，Authenticode `NotSigned`；已通过 Git LFS 纳入仓库，但尚未另建 GitHub Release。本轮按要求只做相关测试和构建，没有重新执行全量门禁或隔离安装冒烟。
- 真实租户 Docx/Wiki 权限、可读范围、revision、撤权和错误表现：尚待验收。
