# 飞书 PM GitHub 发布与新机部署验收

## 目标

完成 TooManyTasks 最新代码的本地部署、旧授权飞书实跑、GitHub 发布，以及从 GitHub 默认入口按新用户流程重新安装验证。

## 当前轮次

2026-08-27：修复应用准备 API 对飞书 Node SDK 顶层 `tenant_access_token` 响应的兼容；补充应用配置身份权限缺失与审批中状态；修正测试装配类型错误；更新安装文档和产品指纹。

## 外部边界

- 企业应用创建仍需飞书官方网页后台。
- 调用应用配置 API 前必须先在应用权限管理中开启 `application:application:patch` 应用身份权限。
- 企业管理员审批、飞书登录、二次验证和 Cindy 宿主安装确认不能自动绕过。
- 旧应用和旧数据库保留，历史治理扫描问题不作为当前功能阻塞。
