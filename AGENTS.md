# AGENTS 协作约定

## 产品最高边界

- 本系统只自动发现、记录和管理任务，不自动执行任务。
- 任何系统主人以外的人能够看到的内容，均属于对外动作，必须由系统主人确认。
- 飞书原文是来源事实，`describe` 是可重新生成的使用层摘要，不能相互替代。
- 文件变化只能说明存在活动或候选产物，不能自动证明任务完成。

## 开工前阅读

1. [README](README.md)
2. [当前状态](docs/current-state.md)：唯一当前事实入口。
3. [项目地图](docs/project-map.md)：按改动区域找到实现、稳定合同、测试位置和文档影响。
4. [文档入口](docs/README.md)：再按本项工作读取必要合同；不要把历史 MVP、CHANGELOG 或 QA 记录当作当前状态。
5. 对应 GitHub Issue 及其明确依赖；未获授权不得创建或修改对外可见内容。

## 开发协作

- 一项工作对应一个 Issue、一个分支和一个 Pull Request。
- 不直接向 `main` 推送未审阅改动。
- README 只保留痛点、能力、用法和文档入口；当前实现、阶段和验证数字只写 `docs/current-state.md`。能力或用法变化才改 README，不要把 Issue 清单或验证数字写回首页。
- 每个可验证里程碑提交前必须检查并同步人读文档：当前实现只更新 `docs/current-state.md`；按影响更新 `docs/user-guide.md`、`docs/architecture.md`、`docs/feishu-integration.md`、`docs/security_and_privacy.md` 和相关 ADR。没有变化的文档要在 PR 清单中明确标记“不适用”，不能默认遗漏。
- 修改产品行为、权限、数据留存或对外动作时，必须同步更新对应文档和验收标准。
- 不提交真实密钥、访问令牌、聊天原文、生产数据库、日志或个人绝对路径。
- 对非技术项目负责人沟通时，先说明用户能看到的结果，再说明必要的技术细节。

## 当前阶段限制

- 阶段、正式载体、源码基线、安装包和验证状态只引用 `docs/current-state.md` 与其中的验证 ID，不在本文件复制。
- 没有真实飞书租户和 LLM 密钥时，只允许使用虚拟数据、契约测试和脱敏诊断；不得声称真实连接已经验证。
- 真实对外动作仍必须经过系统主人确认，桌面化不得放宽产品最高边界。
